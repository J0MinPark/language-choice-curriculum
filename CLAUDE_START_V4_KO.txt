이 폴더를 새 연구 프로젝트로 사용하라. 기존 lexical_choice, jm, jmjm의 다른 프로젝트,
옛 v1/v2/v3 명세·weights·보고서·세션 로그를 검색하거나 이어받지 마라.
이번 기준은 spec/RESEARCH_SPEC_V4_KO.md와 spec/pilot.json이다.

먼저 python3 verify_package.py와 CPU 단위시험을 실행하고, 위 명세 전체를 읽어라.
그 다음 python3 -m freshstart preflight로 인증 상태를 확인하라.
KRDICT_API_KEY는 터미널 환경변수로만 받는다. 키 값/전체 환경/키 포함 URL을 출력하지 마라.
키가 없으면 발급·로컬 설정이라는 사용자 행동을 한 번 요청하고 데이터 수집을 중단하라.
이미 제공된 합법 원본 스냅샷이 있다면 provenance/schema 검사로 대체할 수 있다.
인증 오류를 GPU 문제로 취급하거나 무한 재시도하지 마라.

이번 패키지의 freshstart는 실제 실행 가능한 CPU 참조·수집·보고 도구다.
완성된 LM trainer가 아니므로 implementation/에 새 학습/평가 코드를 구현하라.
원본 spec·reference·tests는 보존하고, 추가 구현·통합시험·lock file을 별도로 작성하라.
의존성은 새 venv 안에서만 설치하고 서버 전체 Torch/CUDA 환경을 바꾸지 마라.
키가 없더라도 코드는 fixture로 구현·단위시험할 수 있지만 fixture를 학습자료/실제결과로 쓰지 마라.

목표는 한국어→영어→중국어→프랑스어 순차학습이다.
어원(문헌 근거), 표기 유사성, 실제 frozen tokenizer의 토큰 유사성을 분리한다.
다국어 의미 대응·어원은 연구자 검수가 필요하다. 에이전트가 인간 승인 기록을 만들지 마라.
UNRESOLVED를 어원 무관으로 바꾸지 말고, 동일 철자 표현은 공유 사건으로 보존하라.
자료가 없으면 API/번역/어원 사실을 지어내지 말고 필요한 검수 항목을 한 곳에 정리하라.

GPU 권한은 v3처럼 일괄0이 아니다. 이 v4의 자료·검수·코드·freeze·재현성 조건을
모두 통과한 경우에만 설정된 한도 안에서 실제 파일럿까지 진행하라.
첫 root는 random INIT부터 T0/T1/T2/T3를 만들고,
T3의 KO입력/RD/dev에서 각 요청언어 A>=0.90 및 측정 검사를 통과해야 한다.
실패하면 다른 root와 H를 확대하지 말고 S 원자료·실패 상태를 보존하라.
예정된 독립4roots를 같은 t0/T3에서 복제하지 마라. 전체 optimizer/RNG/loader를 분기에서 복구하라.
H는 모든 예정 root의 준비·측정이 통과한 뒤 EN–FR 순서만 바꾸며
KO/ZH anchor, 언어당40회, 개념당160round와 content-multiset·tail·laststep을 검증하라.
main_enabled=false는 유지하라. 어원/현재문맥/교육효과의 인과성을 과장하지 마라.

Spearman은 고정된 root/checkpoint/input/format/language/개념집합별로 계산하라.
한 JSON의 실제 수치와 판정에서 표·markdown·최종 답변을 생성하라.
0.539/0.7007 같은 과거 수치를 복사하거나 유리한 평균으로 바꾸지 마라.
최소 한 언어가 기준 미달인데 전체 평균으로 통과시키지 마라.
학습완료, 데이터확보, 단위시험통과, 본시험준비를 서로 다른 상태로 보고하라.

지금 할 일: 새 코드와 실제 가능한 수집/검수 준비부터 수행하고,
허가 조건이 갖춰진 경우에 한해 제한 파일럿까지 진행하라.
마지막에는 work/reports/run_summary.json에서 생성한 짧은 요약을 제공하라.
확인 사실 / 설계 / 가설 / 실제실행 / 미실행·차단 / 다음 사용자 행동을 구분하라.
구현만 완료하고 학습까지 완료했다고 쓰지 마라.
